<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Termination of Employment</title>
    <style>
        @page { size: A4; margin: 0.5in 0.6in 0.4in 0.6in; }
        html, body { margin: 0; padding: 0; background-color: #ffffff; }
        body { 
            font-family: 'Times New Roman', Times, serif; 
            color: #1a1a1a; 
            line-height: 1.45; 
            font-size: 13px; 
        }
        .page-wrapper {
            min-height: 980px;
            position: relative;
            box-sizing: border-box;
            padding-bottom: 55px;
        }
        .header-table {
            width: 100%;
            border-bottom: 2px solid #f27022;
            padding-bottom: 8px;
            margin-bottom: 15px;
        }
        .logo { width: 160px; height: auto; vertical-align: middle; }
        .notice-label {
            font-size: 16px;
            font-weight: bold;
            color: #000;
            text-transform: uppercase;
            text-align: right;
            vertical-align: bottom;
            line-height: 1.2;
        }
        .notice-date { font-size: 11.5px; font-weight: normal; color: #333; }
        .title { 
            text-align: center; 
            font-size: 18px; 
            font-weight: bold; 
            margin: 15px 0 15px 0; 
            color: #000;
            text-transform: uppercase;
            letter-spacing: 0.8px;
        }
        .address-block { margin-bottom: 15px; line-height: 1.4; }
        .employee-name { font-size: 14.5px; font-weight: bold; color: #000; }
        .highlight { font-weight: bold; color: #000; }
        .accent-highlight { font-weight: bold; color: #f27022; }
        .content { text-align: justify; margin-bottom: 15px; }
        .content p { margin: 0 0 10px 0; }
        .signature-table { 
            width: 100%; 
            margin-top: 90px; 
            margin-bottom: 20px;
            border-collapse: collapse;
        }
        .sig-space { height: 55px; margin: 4px 0; }
        .hr-signature-img {
            height: 52px;
            max-width: 180px;
            object-fit: contain;
            display: block;
        }
        .sig-line {
            border-top: 1.5px solid #000000;
            width: 180px;
            margin-top: 4px;
            margin-bottom: 4px;
        }
        .footer-container {
            position: absolute;
            bottom: 0px;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 11px;
            color: #555;
            background-color: #ffffff;
        }
        .footer-line {
            border-top: 1px solid #e0e0e0;
            width: 100%;
            margin: 0 auto 8px auto;
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <table class="header-table">
            <tr>
                <td style="width: 60%;">
                    <img src="<%= logo %>" class="logo" alt="Viral Ads Media">
                </td>
                <td class="notice-label">
                    OFFICIAL NOTICE<br>
                    <span class="notice-date">Date: <%= noticeDate %></span>
                </td>
            </tr>
        </table>

        <div class="address-block">
            To,<br>
            <span class="employee-name highlight"><%= name %></span><br>
            Designation: <%= designation %><br>
            <%= email %><br>
            <%= contact %>
        </div>

        <div class="title">Termination of Employment</div>

        <div class="content">
            <p>Dear <%= name %>,</p>
            <p>This letter serves as formal notification that your employment as <%= designation %> with <span class="highlight">VIRAL ADS MEDIA</span> is terminated effective <span class="accent-highlight"><%= lastWorkingDate %></span>.</p>
            <p>This decision follows a review concerning <%= reason %>.</p>
            <p>Please ensure that all company property, including but not limited to laptop, access cards, and company documents, are returned to the HR department by your final working date. Your final settlement will be processed within the standard company timelines following a full clearance from all departments.</p>
        </div>

        <table class="signature-table">
            <tr>
                <td style="width: 50%;">
                    <p style="margin: 0 0 4px 0;">For <strong>VIRAL ADS MEDIA</strong></p>
                    <div class="sig-space">
                        <% if (locals.hrSignature && hrSignature) { %>
                            <img src="<%= hrSignature %>" alt="HR Signature" class="hr-signature-img">
                        <% } else { %>
                            <div style="height: 45px;"></div>
                        <% } %>
                    </div>
                    <div class="sig-line"></div>
                    <strong><%= typeof hrName !== 'undefined' && hrName ? hrName : 'HR Manager' %></strong><br>
                    Authorised Signatory
                </td>
            </tr>
        </table>

        <div class="footer-container">
            <div class="footer-line"></div>
            <strong>Viral Ads Media</strong> | Digital Creative Agency<br>
            B-27, Budh Vihar Phase 1, New Delhi-86<br>
            9554491854
        </div>
    </div>
</body>
</html>